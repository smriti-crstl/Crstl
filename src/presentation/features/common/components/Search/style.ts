import { Form } from "antd";
import styled from "styled-components";

export const BaseForm = styled(Form)`
  margin-left: 40px;
  display: flex;

  .ant-form-item {
    margin: 0px;
    width: 425px;
  }
`;
